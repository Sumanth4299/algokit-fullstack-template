/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'
import { AppFactory as _AppFactory, AppFactoryAppClientParams, AppFactoryResolveAppClientByCreatorAndNameParams, AppFactoryDeployParams, AppFactoryParams, CreateSchema } from '@algorandfoundation/algokit-utils/types/app-factory'
import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'
import SimulateResponse = modelsv2.SimulateResponse

export const APP_SPEC: Arc56Contract = {"name":"AiMarketplace","structs":{},"methods":[{"name":"post_task","args":[{"type":"pay","name":"payment"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"User calls this to request a task.\nThey must send 1 ALGO (1,000,000 microAlgos) to the contract.","events":[],"recommendations":{}},{"name":"claim_reward","args":[{"type":"address","name":"agent"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"The AI Agent calls this to get paid 1 ALGO from the escrow.","events":[],"recommendations":{}}],"arcs":[22,28],"networks":{},"state":{"schema":{"global":{"ints":1,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{"task_count":{"keyType":"AVMString","valueType":"AVMUint64","key":"dGFza19jb3VudA=="}},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[96],"errorMessage":"check self.task_count exists"},{"pc":[112],"errorMessage":"invalid number of bytes for arc4.static_array<arc4.uint8, 32>"},{"pc":[80],"errorMessage":"transaction type is pay"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"source":{"approval":"","clear":"I3ByYWdtYSB2ZXJzaW9uIDExCiNwcmFnbWEgdHlwZXRyYWNrIGZhbHNlCgovLyBhbGdvcHkuYXJjNC5BUkM0Q29udHJhY3QuY2xlYXJfc3RhdGVfcHJvZ3JhbSgpIC0+IHVpbnQ2NDoKbWFpbjoKICAgIHB1c2hpbnQgMQogICAgcmV0dXJuCg=="},"byteCode":{"approval":"CyADAQDAhD0mAQp0YXNrX2NvdW50MRhAAAMoI2cxG0EAHTEZFEQxGESCAgTaL7MTBKic95w2GgCOAgAJACoAMRkUMRgUEEMxFiIJSTgQIhJESTgHMgoSRDgIJA9EIyhlRCIIKExnIkM2GgFJFYEgEkSxJLIIsgcishAjsgGzIkM=","clear":"C4EBQw=="},"compilerInfo":{"compiler":"puya","compilerVersion":{"major":5,"minor":7,"patch":1}},"events":[],"templateVariables":{}} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the AiMarketplace contract
 */
export type AiMarketplaceArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'post_task(pay)void': {
      payment: AppMethodCallTransactionArgument
    }
    'claim_reward(address)void': {
      agent: string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'post_task(pay)void': [payment: AppMethodCallTransactionArgument]
    'claim_reward(address)void': [agent: string]
  }
}

/**
 * The return type for each method
 */
export type AiMarketplaceReturns = {
  'post_task(pay)void': void
  'claim_reward(address)void': void
}

/**
 * Defines the types of available calls and state of the AiMarketplace smart contract.
 */
export type AiMarketplaceTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'post_task(pay)void' | 'post_task', {
      argsObj: AiMarketplaceArgs['obj']['post_task(pay)void']
      argsTuple: AiMarketplaceArgs['tuple']['post_task(pay)void']
      returns: AiMarketplaceReturns['post_task(pay)void']
    }>
    & Record<'claim_reward(address)void' | 'claim_reward', {
      argsObj: AiMarketplaceArgs['obj']['claim_reward(address)void']
      argsTuple: AiMarketplaceArgs['tuple']['claim_reward(address)void']
      returns: AiMarketplaceReturns['claim_reward(address)void']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    global: {
      keys: {
        taskCount: bigint
      }
      maps: {}
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type AiMarketplaceSignatures = keyof AiMarketplaceTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type AiMarketplaceNonVoidMethodSignatures = keyof AiMarketplaceTypes['methods'] extends infer T ? T extends keyof AiMarketplaceTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the AiMarketplace smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends AiMarketplaceSignatures> = AiMarketplaceTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the AiMarketplace smart contract to the method's return type
 */
export type MethodReturn<TSignature extends AiMarketplaceSignatures> = AiMarketplaceTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed global state of the application.
 */
export type GlobalKeysState = AiMarketplaceTypes['state']['global']['keys']


/**
 * Defines supported create method params for this smart contract
 */
export type AiMarketplaceCreateCallParams =
  | Expand<AppClientBareCallParams & {method?: never} & {onComplete?: OnApplicationComplete.NoOpOC} & CreateSchema>
/**
 * Defines arguments required for the deploy method.
 */
export type AiMarketplaceDeployParams = Expand<Omit<AppFactoryDeployParams, 'createParams' | 'updateParams' | 'deleteParams'> & {
  /**
   * Create transaction parameters to use if a create needs to be issued as part of deployment; use `method` to define ABI call (if available) or leave out for a bare call (if available)
   */
  createParams?: AiMarketplaceCreateCallParams
}>


/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the AiMarketplace smart contract
 */
export abstract class AiMarketplaceParamsFactory {
  /**
   * Constructs a no op call for the post_task(pay)void ABI method
   *
  * User calls this to request a task.
  They must send 1 ALGO (1,000,000 microAlgos) to the contract.

   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static postTask(params: CallParams<AiMarketplaceArgs['obj']['post_task(pay)void'] | AiMarketplaceArgs['tuple']['post_task(pay)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'post_task(pay)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.payment],
    }
  }
  /**
   * Constructs a no op call for the claim_reward(address)void ABI method
   *
   * The AI Agent calls this to get paid 1 ALGO from the escrow.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static claimReward(params: CallParams<AiMarketplaceArgs['obj']['claim_reward(address)void'] | AiMarketplaceArgs['tuple']['claim_reward(address)void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'claim_reward(address)void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.agent],
    }
  }
}

/**
 * A factory to create and deploy one or more instance of the AiMarketplace smart contract and to create one or more app clients to interact with those (or other) app instances
 */
export class AiMarketplaceFactory {
  /**
   * The underlying `AppFactory` for when you want to have more flexibility
   */
  public readonly appFactory: _AppFactory

  /**
   * Creates a new instance of `AiMarketplaceFactory`
   *
   * @param params The parameters to initialise the app factory with
   */
  constructor(params: Omit<AppFactoryParams, 'appSpec'>) {
    this.appFactory = new _AppFactory({
      ...params,
      appSpec: APP_SPEC,
    })
  }
  
  /** The name of the app (from the ARC-32 / ARC-56 app spec or override). */
  public get appName() {
    return this.appFactory.appName
  }
  
  /** The ARC-56 app spec being used */
  get appSpec() {
    return APP_SPEC
  }
  
  /** A reference to the underlying `AlgorandClient` this app factory is using. */
  public get algorand(): AlgorandClient {
    return this.appFactory.algorand
  }
  
  /**
   * Returns a new `AppClient` client for an app instance of the given ID.
   *
   * Automatically populates appName, defaultSender and source maps from the factory
   * if not specified in the params.
   * @param params The parameters to create the app client
   * @returns The `AppClient`
   */
  public getAppClientById(params: AppFactoryAppClientParams) {
    return new AiMarketplaceClient(this.appFactory.getAppClientById(params))
  }
  
  /**
   * Returns a new `AppClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   *
   * Automatically populates appName, defaultSender and source maps from the factory
   * if not specified in the params.
   * @param params The parameters to create the app client
   * @returns The `AppClient`
   */
  public async getAppClientByCreatorAndName(
    params: AppFactoryResolveAppClientByCreatorAndNameParams,
  ) {
    return new AiMarketplaceClient(await this.appFactory.getAppClientByCreatorAndName(params))
  }

  /**
   * Idempotently deploys the AiMarketplace smart contract.
   *
   * @param params The arguments for the contract calls and any additional parameters for the call
   * @returns The deployment result
   */
  public async deploy(params: AiMarketplaceDeployParams = {}) {
    const result = await this.appFactory.deploy({
      ...params,
    })
    return { result: result.result, appClient: new AiMarketplaceClient(result.appClient) }
  }

  /**
   * Get parameters to create transactions (create and deploy related calls) for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Gets available create methods
     */
    create: {
      /**
       * Creates a new instance of the AiMarketplace smart contract using a bare call.
       *
       * @param params The params for the bare (raw) call
       * @returns The params for a create call
       */
      bare: (params?: Expand<AppClientBareCallParams & AppClientCompilationParams & CreateSchema & {onComplete?: OnApplicationComplete.NoOpOC}>) => {
        return this.appFactory.params.bare.create(params)
      },
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Gets available create methods
     */
    create: {
      /**
       * Creates a new instance of the AiMarketplace smart contract using a bare call.
       *
       * @param params The params for the bare (raw) call
       * @returns The transaction for a create call
       */
      bare: (params?: Expand<AppClientBareCallParams & AppClientCompilationParams & CreateSchema & {onComplete?: OnApplicationComplete.NoOpOC}>) => {
        return this.appFactory.createTransaction.bare.create(params)
      },
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Gets available create methods
     */
    create: {
      /**
       * Creates a new instance of the AiMarketplace smart contract using a bare call.
       *
       * @param params The params for the bare (raw) call
       * @returns The create result
       */
      bare: async (params?: Expand<AppClientBareCallParams & AppClientCompilationParams & CreateSchema & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}>) => {
        const result = await this.appFactory.send.bare.create(params)
        return { result: result.result, appClient: new AiMarketplaceClient(result.appClient) }
      },
    },

  }

}
/**
 * A client to make calls to the AiMarketplace smart contract
 */
export class AiMarketplaceClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `AiMarketplaceClient`
   *
   * @param appClient An `AppClient` instance which has been created with the AiMarketplace app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `AiMarketplaceClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }
  
  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends AiMarketplaceNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }
  
  /**
   * Returns a new `AiMarketplaceClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<AiMarketplaceClient> {
    return new AiMarketplaceClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `AiMarketplaceClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<AiMarketplaceClient> {
    return new AiMarketplaceClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the AiMarketplace smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the AiMarketplace smart contract using the `post_task(pay)void` ABI method.
     *
    * User calls this to request a task.
    They must send 1 ALGO (1,000,000 microAlgos) to the contract.

     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    postTask: (params: CallParams<AiMarketplaceArgs['obj']['post_task(pay)void'] | AiMarketplaceArgs['tuple']['post_task(pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AiMarketplaceParamsFactory.postTask(params))
    },

    /**
     * Makes a call to the AiMarketplace smart contract using the `claim_reward(address)void` ABI method.
     *
     * The AI Agent calls this to get paid 1 ALGO from the escrow.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    claimReward: (params: CallParams<AiMarketplaceArgs['obj']['claim_reward(address)void'] | AiMarketplaceArgs['tuple']['claim_reward(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(AiMarketplaceParamsFactory.claimReward(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the AiMarketplace smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the AiMarketplace smart contract using the `post_task(pay)void` ABI method.
     *
    * User calls this to request a task.
    They must send 1 ALGO (1,000,000 microAlgos) to the contract.

     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    postTask: (params: CallParams<AiMarketplaceArgs['obj']['post_task(pay)void'] | AiMarketplaceArgs['tuple']['post_task(pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AiMarketplaceParamsFactory.postTask(params))
    },

    /**
     * Makes a call to the AiMarketplace smart contract using the `claim_reward(address)void` ABI method.
     *
     * The AI Agent calls this to get paid 1 ALGO from the escrow.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    claimReward: (params: CallParams<AiMarketplaceArgs['obj']['claim_reward(address)void'] | AiMarketplaceArgs['tuple']['claim_reward(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(AiMarketplaceParamsFactory.claimReward(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the AiMarketplace smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the AiMarketplace smart contract using the `post_task(pay)void` ABI method.
     *
    * User calls this to request a task.
    They must send 1 ALGO (1,000,000 microAlgos) to the contract.

     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    postTask: async (params: CallParams<AiMarketplaceArgs['obj']['post_task(pay)void'] | AiMarketplaceArgs['tuple']['post_task(pay)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AiMarketplaceParamsFactory.postTask(params))
      return {...result, return: result.return as unknown as (undefined | AiMarketplaceReturns['post_task(pay)void'])}
    },

    /**
     * Makes a call to the AiMarketplace smart contract using the `claim_reward(address)void` ABI method.
     *
     * The AI Agent calls this to get paid 1 ALGO from the escrow.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    claimReward: async (params: CallParams<AiMarketplaceArgs['obj']['claim_reward(address)void'] | AiMarketplaceArgs['tuple']['claim_reward(address)void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(AiMarketplaceParamsFactory.claimReward(params))
      return {...result, return: result.return as unknown as (undefined | AiMarketplaceReturns['claim_reward(address)void'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new AiMarketplaceClient(this.appClient.clone(params))
  }

  /**
   * Methods to access state for the current AiMarketplace app
   */
  state = {
    /**
     * Methods to access global state for the current AiMarketplace app
     */
    global: {
      /**
       * Get all current keyed values from global state
       */
      getAll: async (): Promise<Partial<Expand<GlobalKeysState>>> => {
        const result = await this.appClient.state.global.getAll()
        return {
          taskCount: result.task_count,
        }
      },
      /**
       * Get the current value of the task_count key in global state
       */
      taskCount: async (): Promise<bigint | undefined> => { return (await this.appClient.state.global.getValue("task_count")) as bigint | undefined },
    },
  }

  public newGroup(): AiMarketplaceComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a post_task(pay)void method call against the AiMarketplace contract
       */
      postTask(params: CallParams<AiMarketplaceArgs['obj']['post_task(pay)void'] | AiMarketplaceArgs['tuple']['post_task(pay)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.postTask(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a claim_reward(address)void method call against the AiMarketplace contract
       */
      claimReward(params: CallParams<AiMarketplaceArgs['obj']['claim_reward(address)void'] | AiMarketplaceArgs['tuple']['claim_reward(address)void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.claimReward(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a clear state call to the AiMarketplace contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as AiMarketplaceComposer
  }
}
export type AiMarketplaceComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the post_task(pay)void ABI method.
   *
  * User calls this to request a task.
  They must send 1 ALGO (1,000,000 microAlgos) to the contract.

   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  postTask(params?: CallParams<AiMarketplaceArgs['obj']['post_task(pay)void'] | AiMarketplaceArgs['tuple']['post_task(pay)void']>): AiMarketplaceComposer<[...TReturns, AiMarketplaceReturns['post_task(pay)void'] | undefined]>

  /**
   * Calls the claim_reward(address)void ABI method.
   *
   * The AI Agent calls this to get paid 1 ALGO from the escrow.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  claimReward(params?: CallParams<AiMarketplaceArgs['obj']['claim_reward(address)void'] | AiMarketplaceArgs['tuple']['claim_reward(address)void']>): AiMarketplaceComposer<[...TReturns, AiMarketplaceReturns['claim_reward(address)void'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the AiMarketplace smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): AiMarketplaceComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): AiMarketplaceComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<AiMarketplaceComposerResults<TReturns> & { simulateResponse: SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<AiMarketplaceComposerResults<TReturns> & { simulateResponse: SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<AiMarketplaceComposerResults<TReturns> & { simulateResponse: SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<AiMarketplaceComposerResults<TReturns>>
}
export type AiMarketplaceComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

